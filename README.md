# tiboeuf
Simple bash script for listen web radio from list.

## Notes
* Only one dependency is required : `mpv`
* `radio.db` file must be in same directory of tiboeuf.sh
* `radio.db` file must be filled in as follows: `Radio title|Genre or short description|Stream URL`
